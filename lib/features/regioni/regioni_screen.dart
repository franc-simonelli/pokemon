import 'package:flutter/material.dart';

class RegioniScreen extends StatelessWidget {
  const RegioniScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
